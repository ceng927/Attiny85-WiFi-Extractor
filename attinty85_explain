## ATtiny85 微控制器 硬軟體搜尋電腦網路名稱、密碼的紀錄 
**針對實體接觸攻擊與 HID 的資訊安全研究**

專題利用的 **AVR微控制器 (ATtiny85)** 模擬 USB 鍵盤（HID 裝置），作業系統面對惡意硬體（ BadUSB / USB 橡皮鴨）自動化輸入攻擊

---

##專題研發動機
在資安領域中，被大家討論的常常是防火牆以及各種軟體的問題，但是大家卻都忽略了硬體的實體攻擊方式

這個專題在討論如何利用微控制器偽裝成合法輸入裝置，在不處碰到防毒軟體的情況下，快速、自動地檢測與提取系統內部配置。

---

##技術架構與核心技術

本專題的核心**「軟硬體整合」**跟**「自動化腳本時序優化」**，主要有兩個核心技術層面：

### 1. 軟硬體與 HID 模擬 
 **硬體核心**： **DigiSpark ATtiny85** 開發板，具備微型化、低成本與內建 USB 介面的特點。
 **軟體編寫**： **C/C++ (Arduino IDE)** 開發，引入 DigiKeyboard.h 函式庫，將attiny85列為符合規範的 HID (Human Interface Device) 虛擬鍵盤
 **電腦反應與時間差的問題**：
  在不同效能的電腦上進行實測後，發現老舊電腦對於鍵盤輸入的反應速度存在差異。為了確保過程中可以10%執行成功，我設計了 延遲機制（delay）。在關鍵步驟像 Win + R 喚醒系統、啟動終端機後加入合理的等待秒數，確保指令不會因為電腦卡頓而中斷或輸入錯位

### 2. 作業系統終端機評估與 .NET 管線優化 (OS & Scripting Layer)
在撰寫自動化指令時，本專題深入比較並探討了 Windows 系統的核心終端機架構：
* **CMD 與 PowerShell 的決策抉擇**：
  傳統的命令提示字元 (CMD) 只能處理單純的文字，若要進行複雜的資料解析，需要撰寫極度冗長且容易出錯的批次檔，這時候就要用到powershell了
* **底層 .NET 架構的深度應用**：
  本專題選擇直接呼叫 **PowerShell**。PowerShell 於 **.NET 物件架構**，讓腳本能夠直接操作物件，大幅提升資料處理效率。
  * **編碼修正（解決中文亂碼）**：呼叫 .NET 的類別 [Console]::OutputEncoding = [System.Text.Encoding]::UTF8，成功解決台灣環境下常見的**中文 SSID 亂碼問題**。
  * **資料篩選**：利用 .NET 的字串處理方法與 ForEach-Object 迴圈，將複雜的系統設定檔即時解析，精準格式化提取出 Wi-Fi 名稱與對應的明碼密碼。

---

## 未來展望 
在進入大學資工/資安相關學系後，本專題預計將朝向以下方向深入擴展：
1. **隱蔽性提升**：深入探討如何利用 -WindowStyle Hidden 參數優化腳本，並研究如何繞過端點偵測與回應系統的行為偵測
2. **自動化資料回傳**：結合網路程式設計（Web Programming），利用 Raspberry Pi 將檢測結果自動以加密方式回傳至後端伺服器
